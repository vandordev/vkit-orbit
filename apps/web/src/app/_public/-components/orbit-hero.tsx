import { motion, useReducedMotion } from "framer-motion";

export function OrbitHero() {
	const reducedMotion = useReducedMotion();
	return <section className="mx-auto grid max-w-6xl gap-12 px-6 pb-24 pt-16 lg:grid-cols-[1fr_0.9fr] lg:items-center"><div className="max-w-2xl space-y-7"><p className="font-mono text-xs uppercase tracking-[0.35em] text-cyan-300">A domain-neutral runtime constellation</p><h1 className="text-5xl font-semibold tracking-[-0.06em] text-white sm:text-7xl">Build your product at the center of the orbit.</h1><p className="max-w-xl text-lg leading-8 text-slate-400">Vkit Orbit is a TanStack Start boilerplate with embedded Elysia, Prisma, River producers, a Go worker, and optional realtime—clear boundaries for the domain you bring next.</p></div><div className="relative mx-auto aspect-square w-full max-w-[30rem]" aria-label="Runtime boundaries orbit diagram" role="img"><div className="absolute inset-[12%] rounded-full border border-cyan-300/20" /><div className="absolute inset-[25%] rounded-full border border-indigo-300/20" /><motion.div className="absolute inset-[38%] grid place-items-center rounded-full border border-cyan-200/50 bg-cyan-200/10 text-center shadow-[0_0_80px_rgba(103,232,249,0.18)]" animate={reducedMotion ? undefined : { scale: [1, 1.04, 1] }} transition={{ duration: 6, repeat: Infinity, ease: "easeInOut" }}><span className="font-mono text-xs tracking-[0.2em] text-cyan-100">VKIT<br />ORBIT</span></motion.div>{["WEB", "API", "DB", "WORKER"].map((label, index) => <span key={label} className={`absolute rounded-full border border-slate-700 bg-[#111827] px-3 py-2 font-mono text-[10px] text-slate-300 ${["left-0 top-1/2", "right-0 top-1/4", "bottom-0 left-1/2", "left-1/4 top-0"][index]}`}>{label}</span>)}</div></section>;
}
