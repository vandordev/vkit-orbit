const boundaries = ["TanStack Start", "Embedded Elysia", "Prisma", "River", "Go worker", "Realtime (optional)"];

export function ArchitectureMap() {
	return <section className="border-t border-white/10 bg-[#0d1220] px-6 py-24"><div className="mx-auto max-w-6xl space-y-10"><div className="max-w-2xl space-y-3"><p className="font-mono text-xs uppercase tracking-[0.3em] text-indigo-300">The actual topology</p><h2 className="text-3xl font-semibold tracking-tight text-white sm:text-4xl">One public server. Focused runtime boundaries.</h2><p className="leading-7 text-slate-400">Browser traffic stays same-origin. TanStack Start embeds Elysia directly; Elysia owns business responses. Supporting runtimes enqueue and consume work without becoming a second public API.</p></div><div className="grid gap-px overflow-hidden rounded-2xl border border-white/10 bg-white/10 sm:grid-cols-2 lg:grid-cols-3">{boundaries.map((label, index) => <div key={label} className="min-h-36 bg-[#0d1220] p-6"><span className="font-mono text-xs text-cyan-300">0{index + 1}</span><h3 className="mt-8 text-lg font-medium text-slate-100">{label}</h3><p className="mt-2 text-sm text-slate-500">{index === 0 ? "Public runtime and UI routes" : index === 1 ? "Embedded business HTTP boundary" : index === 2 ? "Schema and client ownership" : "Supporting runtime boundary"}</p></div>)}</div><p className="font-mono text-sm text-slate-500">Browser → TanStack Start + embedded Elysia → Prisma / PostgreSQL</p></div></section>;
}
