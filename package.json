{
  "name": "turbostack",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.3.14",
  "scripts": {
    "build": "bun run check-types && turbo run build",
    "start": "bun run start:web",
    "start:web": "cd apps/web && bun run start",
    "start:scheduler": "cd apps/scheduler && bun run start",
    "start:realtime": "cd apps/realtime && bun run start",
    "dev:web": "turbo run dev --filter=web",
    "dev:scheduler": "turbo run dev --filter=@repo/scheduler",
    "dev:realtime": "turbo run dev --filter=@repo/realtime-server",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "check-types": "turbo run check-types",
    "docker:build:web": "turbo run build --filter=web",
    "clean": "turbo run clean",
    "prepare": "husky"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "husky": "^9.1.7",
    "prettier": "^3.7.4",
    "turbo": "^2.8.1",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=22"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {}
}
